/**
 * The value the npm registry stores in a packument's `readme` field when the
 * published tarball had no README it recognised at publish time. It is not
 * README content: a stored copy counts as a missing README.
 */
export const NPM_MISSING_README_PLACEHOLDER = 'ERROR: No README data found!';
